# Calling - Multiple Configurations

The multiple configurations setup is useful for organizations that has infrastructure that is not necassarily the same in each environment.

This concept is used for the idea of core / shared infrastructure  across the organization that does not require a route-to-live, but has core services used in each environment.

Resources that might use the configuration could be networking resources, logging resources, security resources.
